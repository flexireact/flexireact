import { defineConfig } from 'flexireact';

export default defineConfig({
  // Enable TypeScript
  typescript: true,
  
  // Tailwind CSS integration
  tailwind: true,
  
  // Default to dark mode
  theme: 'dark',
});
